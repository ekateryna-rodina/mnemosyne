apiVersion: apps/v1
kind: Deployment
metadata: apps
  name: mobile-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mobile
  template:
    metadata:
      labels:
        app: mobile
    spec:
      containers:
        name: mobile
        image: krodina/mobile